services:
  backend:
    build: ./server
    ports:
      - "3000:3000"
    env_file:
      - .env
  
  frontend:
    build: ./client
    ports:
      - "5173:5173"
    env_file:
      - .env
    depends_on:
      - backend